grafana:
  adminPassword: changeme

kubelet:
  namespace: prometheus-operator

prometheus:
  prometheusSpec:
    retention: 30d

prometheusOperator:
  kubeletService:
    namespace: prometheus-operator
