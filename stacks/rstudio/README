1. Verify that RStudio is running with
kubectl get pods

2. Forward port 8787 with
kubectl port-forward deploy/rstudio 8787

3. change the user/password (defaults to rstudio/rstudio) from RStudio terminal on localhost:8787
passwd rstudio
